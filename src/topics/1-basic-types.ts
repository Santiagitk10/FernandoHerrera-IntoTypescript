let name: string = 'Strider';
//Le puedo indicar que la variable va a tomar valores numéricos y uno exacto como 'FULL'
let hpPoints: number | 'FULL' = 95;
let isAlive: boolean = true;


console.log({
    name, hpPoints, isAlive
});

export {};